// write your actions creator and reducers here
// This is a fake reducer replace body of it with your code (josphat)
const missionsReducer = () => 'hi from fake reducer for missions';
export default missionsReducer;
